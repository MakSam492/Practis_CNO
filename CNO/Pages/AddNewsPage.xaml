<Page x:Class="CNO.Pages.AddNewsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CNO.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="700"
    Title="Новость"
      FontFamily="Segoe UI">

    <Page.Resources>
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#FFF0F2F5"/>
        <!-- Светлый фон для всей страницы -->
        <SolidColorBrush x:Key="FormBackgroundBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="PrimaryAccentBrush" Color="#FF0078D4"/>
        <!-- Более стандартный синий акцент -->
        <SolidColorBrush x:Key="PrimaryAccentHoverBrush" Color="#FF005A9E"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#FF201F1E"/>
        <!-- Почти черный -->
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#FF605E5C"/>
        <!-- Серый -->
        <SolidColorBrush x:Key="BorderBrush" Color="#FFC8C6C4"/>
        <!-- Светло-серый для границ -->
        <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="#FFF3F2F1"/>
        <SolidColorBrush x:Key="ErrorBorderBrush" Color="#FFA80000"/>

        <SolidColorBrush x:Key="SaveButtonBackgroundBrush" Color="Blue"/>
        <SolidColorBrush x:Key="SaveButtonHoverBackgroundBrush" Color="Blue"/>
        <SolidColorBrush x:Key="CancelButtonBackgroundBrush" Color="#FFD83B01"/>
        <!-- Оранжево-красный для отмены -->
        <SolidColorBrush x:Key="CancelButtonHoverBackgroundBrush" Color="#FFBF3501"/>
        <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#FFFFFFFF"/>


        <Style x:Key="PageTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,25"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <!-- Сделал жирнее для лучшего выделения -->
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <ControlTemplate x:Key="TextBoxErrorTemplate">
            <AdornedElementPlaceholder Name="adorner"/>
            <!-- Можно добавить ToolTip для вывода ошибки, если нужно
            <ToolTipService.ToolTip>
                <ToolTip Content="{Binding ElementName=adorner, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"/>
            </ToolTipService.ToolTip>
            -->
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource FormBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryAccentBrush}"/>
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource TextBoxErrorTemplate}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryAccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <!-- Убрал тень, чтобы было чище, оставил выделение рамкой -->
                            </Trigger>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ErrorBorderBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DisabledBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource ButtonForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="PART_Border" Background="{TemplateBinding Background}" CornerRadius="4" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PART_Border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource DisabledBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Border Background="{StaticResource FormBackgroundBrush}" 
                MaxWidth="650" Padding="35,30,35,30" Margin="20"
                VerticalAlignment="Center" HorizontalAlignment="Center"
                CornerRadius="6"
                >

            <StackPanel Orientation="Vertical">
                <TextBlock x:Name="PageTitle" Text="Создание новости" Style="{StaticResource PageTitleStyle}"/>

                <TextBlock Text="Заголовок новости" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="txtNewsTitle" Margin="0,0,0,20"
                         d:Text="Пример заголовка новости"/>

                <TextBlock Text="Текст новости" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="txtNewsDescription" Margin="0,0,0,30" 
                         TextWrapping="Wrap" AcceptsReturn="True" MinHeight="120" MaxHeight="300"
                         VerticalScrollBarVisibility="Auto"
                         d:Text="Полное описание новости здесь..."/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="btnSaveNews" Content="Сохранить" Click="BtnSaveNews_Click" 
                            Style="{StaticResource ActionButtonStyle}" Background="{StaticResource SaveButtonBackgroundBrush}"/>
                    <Button x:Name="btnCancel" Content="Отмена" Click="BtnCancel_Click" 
                            Style="{StaticResource ActionButtonStyle}" Background="{StaticResource CancelButtonBackgroundBrush}" Margin="0"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>