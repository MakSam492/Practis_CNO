<Page x:Class="CNO.Pages.DoctorsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:CNO.Pages"
      xmlns:viewModels="clr-namespace:CNO.ViewModel"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=local:DoctorsPage, IsDesignTimeCreatable=True}"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Список врачей">

    <Page.Resources>
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#FFF4F6F8"/>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="HeaderTextBrush" Color="#FF333333"/>
        <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#FF00A2E8"/>
        <SolidColorBrush x:Key="PrimaryButtonHoverBrush" Color="#FF008FD8"/>
        <SolidColorBrush x:Key="LightTextBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="SubtleBorderBrush" Color="#FFDDE0E4"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#FFEBEEF0"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FF4A4A4A"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FF7F8C9A"/>
        <SolidColorBrush x:Key="AccentColorBrush" Color="#FF0078D4"/>

        <Style x:Key="DoctorCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" Direction="270" Color="#1A000000" Opacity="0.1" BlurRadius="5"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentColorBrush}"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect ShadowDepth="3" Direction="270" Color="#2A000000" Opacity="0.15" BlurRadius="7"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="DoctorItemTemplate" DataType="{x:Type viewModels:DoctorViewModel}">
            <Border Style="{StaticResource DoctorCardStyle}" Cursor="Hand" MouseLeftButtonDown="DoctorCard_MouseLeftButtonDown">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding DoctorName}" FontSize="18" FontWeight="SemiBold" 
                               Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>

                    <TextBlock Grid.Row="0" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <Run Text="ID:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                        <Run Text="{Binding ID}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentColorBrush}"/>
                    </TextBlock>

                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,8,0,5">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14"/>
                        <TextBlock Text="{Binding Spez}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,0">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14"/>
                        <TextBlock Text="{Binding Num}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>

    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- Оставил эту строку для возможной панели фильтров, как в вашем исходном коде -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource HeaderBackgroundBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Click="BackButton_Click" ToolTip="Назад" Background="Transparent" BorderThickness="0" Padding="0" Margin="0,0,20,0" VerticalAlignment="Center" Cursor="Hand">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="22" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Button>

                <TextBlock Grid.Column="1" Text="Список врачей" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource HeaderTextBrush}" VerticalAlignment="Center"/>

                <Button Grid.Column="2" Click="AddDoctorButton_Click" Padding="15,10" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource LightTextBrush}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="16"/>
                                                <ContentPresenter/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource PrimaryButtonHoverBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="Добавить врача"/>
                </Button>
            </Grid>
        </Border>

        <!-- Панель для фильтров (если нужна) - вы можете добавить сюда элементы управления -->
        <Border Grid.Row="1" Padding="10" Margin="10,10,10,0" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="5"
                Visibility="Collapsed">
            <!-- Пока скрыта, установите Visible, если будете использовать -->
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Фильтры:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <!-- Добавьте сюда TextBox, ComboBox и кнопку FilterButton_Click -->
                <TextBox Width="200" Margin="0,0,10,0" ToolTip="Поиск по ФИО..."/>
                <ComboBox Width="150" Margin="0,0,10,0" ToolTip="Фильтр по специальности"/>
                <Button Content="Применить фильтр" Click="FilterButton_Click"/>
            </StackPanel>
        </Border>


        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="20,15,20,20">
            <ItemsControl x:Name="DoctorsListView" ItemTemplate="{StaticResource DoctorItemTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>