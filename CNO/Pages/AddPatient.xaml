<Page x:Class="CNO.Pages.AddPatient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="550" d:DesignWidth="500" 
      Title="Добавление пациента"
      FontFamily="Segoe UI">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryHeaderBrush" Color="#FF2D3A4D"/>
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#FFE9ECEF"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="White"/>
        <SolidColorBrush x:Key="LightTextBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="DarkTextBrush" Color="#FF343A40"/>
        <SolidColorBrush x:Key="SubtleBorderBrush" Color="#FFDEE2E6"/>
        <SolidColorBrush x:Key="PrimaryActionTextBrush" Color="White"/>
        <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#FF007BFF"/>
        <SolidColorBrush x:Key="PrimaryButtonHoverBrush" Color="#FF0056B3"/>
        <SolidColorBrush x:Key="AccentButtonBrush" Color="#FF28A745"/>
        <SolidColorBrush x:Key="AccentButtonHoverBrush" Color="#FF1E7E34"/>
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#FF6C757D"/>
        <SolidColorBrush x:Key="SecondaryButtonHoverBrush" Color="#FF5A6268"/>

        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryButtonBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0"  Opacity="0.5" BlurRadius="4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0"
                                                   Focusable="{TemplateBinding Focusable}"
                                                   HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                   VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                   Padding="{TemplateBinding Padding}"/>
                                <Button x:Name="PART_Button" Grid.Column="1"
                                        Focusable="False"
                                        Foreground="{TemplateBinding Foreground}"
                                        FontFamily="Segoe MDL2 Assets" Content=""
                                        Width="{Binding Height, ElementName=PART_TextBox, Mode=OneWay}"
                                        Style="{DynamicResource {x:Static ToolBar.ButtonStyleKey}}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryButtonBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0"  Opacity="0.5" BlurRadius="4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormActionButtonBase" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource PrimaryActionTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

        <Style x:Key="AddPatientActionButton" TargetType="Button" BasedOn="{StaticResource FormActionButtonBase}">
            <Setter Property="Background" Value="{StaticResource AccentButtonBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentButtonHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelPatientActionButton" TargetType="Button" BasedOn="{StaticResource FormActionButtonBase}">
            <Setter Property="Background" Value="{StaticResource SecondaryButtonBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource LightTextBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryButtonHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryHeaderBrush}" Padding="20,15">
            <TextBlock Text="Добавление нового пациента"
                       FontSize="22"
                       Foreground="{StaticResource LightTextBrush}"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <Border Background="{StaticResource CardBackgroundBrush}"
                    Margin="25"
                    Padding="25"
                    CornerRadius="8"
                    BorderBrush="{StaticResource SubtleBorderBrush}"
                    BorderThickness="1"
                    MaxWidth="450"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Color="#FFBBBBBB" Opacity="0.3"/>
                </Border.Effect>

                <StackPanel>
                    <Label Content="Фамилия:"/>
                    <TextBox x:Name="LastNameBox"/>

                    <Label Content="Имя:"/>
                    <TextBox x:Name="FirstNameBox"/>

                    <Label Content="Номер телефона:"/>
                    <TextBox x:Name="PhoneNumber"/>

                    <Label Content="Дата рождения:"/>
                    <DatePicker x:Name="BirthDatePicker"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="CancelButton" Content="Отмена" 
                                Style="{StaticResource CancelPatientActionButton}"
                                Click="CancelButton_Click" Margin="0,0,10,0"/>
                        <Button x:Name="AddButton" Content="Добавить" 
                                Style="{StaticResource AddPatientActionButton}"
                                Click="AddButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Page>