<Page x:Class="CNO.Pages.AddDoctorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="400"
      Title="Добавление врача">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#FF3F51B5" Padding="10" CornerRadius="5">
            <TextBlock Text="Добавление нового врача" 
                       FontSize="16" 
                       Foreground="White" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Форма ввода -->
        <StackPanel Grid.Row="1" Margin="0,20,0,0">
            <Label Content="Фамилия*" FontWeight="Bold"/>
            <TextBox x:Name="LastNameBox" Margin="0,5,0,15"/>

            <Label Content="Имя*" FontWeight="Bold"/>
            <TextBox x:Name="FirstNameBox" Margin="0,5,0,15"/>

           

            <Label Content="Специальность*" FontWeight="Bold"/>
            <TextBox x:Name="SpecializationBox" Margin="0,5,0,15"/>

           
            <Label Content="Телефон"/>
            <TextBox x:Name="PhoneBox" Margin="0,5,0,20"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" 
                    Width="100" 
                    Margin="0,0,10,0" 
                    Click="CancelButton_Click"/>
            <Button Content="Сохранить" 
                    Width="100" 
                    Click="SaveButton_Click" 
                    Background="#FF4CAF50" 
                    Foreground="White"/>
        </StackPanel>
    </Grid>
</Page>