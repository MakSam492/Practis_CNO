<Page x:Class="CNO.Pages.PatientDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CNO.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="Подробная информация о пациенте"
      FontFamily="Segoe UI">

    <Page.Resources>
        <!-- Цвета, определенные локально для этой страницы -->
        <SolidColorBrush x:Key="PrimaryHeaderBrush" Color="#FF2D3A4D"/>
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#FFE9ECEF"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="White"/>
        <SolidColorBrush x:Key="LightTextBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="DarkTextBrush" Color="#FF343A40"/>
        <SolidColorBrush x:Key="SubtleBorderBrush" Color="#FFDEE2E6"/>
        <SolidColorBrush x:Key="LabelTextBrush" Color="#FF495057"/>
        <SolidColorBrush x:Key="PrimaryActionTextBrush" Color="White"/>

        <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#FF007BFF"/>
        <SolidColorBrush x:Key="PrimaryButtonHoverBrush" Color="#FF0056B3"/>
        <SolidColorBrush x:Key="AccentButtonBrush" Color="#FF28A745"/>
        <SolidColorBrush x:Key="AccentButtonHoverBrush" Color="#FF1E7E34"/>
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#FF6C757D"/>
        <SolidColorBrush x:Key="SecondaryButtonHoverBrush" Color="#FF5A6268"/>

        <!-- Стили элементов -->
        <Style TargetType="TextBlock" x:Key="DetailLabelStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource LabelTextBrush}"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="120"/>
        </Style>

        <Style TargetType="TextBox" x:Key="EditableDetailValueStyle">
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="PART_Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="PART_Border" Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Padding" Value="0,5"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="False">
                                <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource PrimaryButtonBrush}"/>
                                <Setter TargetName="PART_Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0"  Opacity="0.5" BlurRadius="4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormActionButtonBase" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource PrimaryActionTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="100"/>
            
        </Style>

        <Style x:Key="EditActionButton" TargetType="Button" BasedOn="{StaticResource FormActionButtonBase}">
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryButtonHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SaveActionButton" TargetType="Button" BasedOn="{StaticResource FormActionButtonBase}">
            <Setter Property="Background" Value="{StaticResource AccentButtonBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentButtonHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CancelEditActionButton" TargetType="Button" BasedOn="{StaticResource FormActionButtonBase}">
            <Setter Property="Background" Value="{StaticResource SecondaryButtonBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource LightTextBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryButtonHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="InfoCardStyle">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="6" Color="#FFBBBBBB" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderBackButton" TargetType="Button" BasedOn="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource LightTextBrush}"/>
            <Setter Property="ToolTip" Value="Назад"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryHeaderBrush}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Click="BackButton_Click" Style="{StaticResource HeaderBackButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="Назад" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <TextBlock Grid.Column="1" Text="Карточка пациента"
                           FontSize="22" Foreground="{StaticResource LightTextBrush}" FontWeight="SemiBold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="EditButton" Content="Редактировать" Style="{StaticResource EditActionButton}" Click="EditButton_Click" Visibility="Visible"/>
                    <Button x:Name="SaveButton" Content="Сохранить" Style="{StaticResource SaveActionButton}" Click="SaveButton_Click" Visibility="Collapsed" Margin="5,0,0,0"/>
                    <Button x:Name="CancelButton" Content="Отмена" Style="{StaticResource CancelEditActionButton}" Click="CancelEditButton_Click" Visibility="Collapsed" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource InfoCardStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="PatientInfoPanel">
                        <TextBlock Text="Личная информация" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="ID Пациента:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="PatientIdTextBox" IsReadOnly="True" Background="Transparent" BorderThickness="0"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="Фамилия:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="LastNameTextBox" IsReadOnly="True"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="Имя:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="FirstNameTextBox" IsReadOnly="True"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="Отчество:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="MiddleNameTextBox" IsReadOnly="True"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="Дата рождения:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="DateOfBirthTextBox" IsReadOnly="True" Background="Transparent" BorderThickness="0"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="Пол:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="GenderTextBox" IsReadOnly="True"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="Номер телефона:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="PhoneNumberTextBox" IsReadOnly="True"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource DetailLabelStyle}" Text="Адрес:"/>
                            <TextBox Grid.Row="1" Style="{StaticResource EditableDetailValueStyle}" x:Name="AddressTextBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60" VerticalScrollBarVisibility="Auto"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource InfoCardStyle}">
                <StackPanel>
                    <TextBlock Text="История записей" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkTextBrush}" Margin="0,0,0,15"/>
                    <ListView x:Name="MedicalHistoryListView" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.View>
                            <GridView>
                                <GridView.ColumnHeaderContainerStyle>
                                    <Style TargetType="GridViewColumnHeader">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="10,8"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
                                    </Style>
                                </GridView.ColumnHeaderContainerStyle>
                                <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding RecordDate, StringFormat='dd.MM.yyyy HH:mm'}" Width="140"/>
                                <GridViewColumn Header="Врач" DisplayMemberBinding="{Binding DoctorFullName}" Width="180"/>
                                <GridViewColumn Header="Описание приема" DisplayMemberBinding="{Binding DiagnosisOrNotes}"/>
                            </GridView>
                        </ListView.View>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="{StaticResource PageBackgroundBrush}"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>